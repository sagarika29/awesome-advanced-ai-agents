# Shared Contracts

JSON schemas shared between the Python agent API and the React Native shell.

- `events.schema.json` — streaming shell event envelope
- `persona.schema.json` — OGs of Tech persona metadata

Python mirrors live in `agents/core/events.py`. Mobile types live in
`apps/mobile/src/types/contracts.ts`.
